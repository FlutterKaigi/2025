{
  "name": "bridge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev -e staging",
    "cf-typegen": "wrangler types",
    "check": "biome check --write ."
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.14.1",
    "@biomejs/biome": "^1.9.4"
  }
}
