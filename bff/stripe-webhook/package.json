{
  "name": "stripe-webhook",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.8.5",
    "stripe": "^18.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}