import 'package:jaspr/jaspr.dart';

// As your css styles are defined using just Dart, you can simply
// use global variables or methods for common things like colors.
const beyondRed = Color.variable('--color-beyond-red');
const flutterSky = Color.variable('--color-flutter-sky');
const gradientCenter = Color.variable('--color-gradient-center');
const linkColor = Colors.steelBlue;
const borderColor = Colors.slateGray;
const backgroundColor = Colors.white;
const textColor = Color('#121212');
